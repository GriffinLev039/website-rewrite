const sigPohot = document.getElementById("significantIMG");
const imgContainer = document.getElementById("StickImage");

document.addEventListener('scroll',()=>{
    console.log(imgContainer.scrollTop);
})